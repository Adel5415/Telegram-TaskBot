# Telegram TaskBot

**Автор:** Адель Музафаров

Телеграм-бот для управления задачами (to-do) с поддержкой дедлайнов, приоритетов, напоминаний и удобным интерфейсом.
Работает на Python и aiogram 3.x. Все задачи сохраняются между перезапусками, бот поддерживает множество функций и интуитивно понятен даже новичку.

## 📌 Основные возможности
- Добавление задач с текстом, дедлайном (датой и временем) и приоритетом. 
- Просмотр задач со статусами, дедлайнами и приоритетами.
- Отметка задач выполненными.
- Удаление задач.
- Изменение текста задачи.
- Изменение дедлайна и приоритета.
- Поиск по задачам.
- Сортировка задач по дедлайну, приоритету и статусу.
- Очистка всех задач.
- Автоматические напоминания за 30 минут до дедлайна.
- Поддержка нескольких пользователей (у каждого свой список задач).
- Полная поддержка русского языка.

## 🚀 Быстрый старт
### 1. Клонируй репозиторий:
```bash

git clone https://github.com/Adel5415/Telegram-TaskBot.git
cd TelegramTaskBot
```
### 2. Установи зависимости:
Требуется Python 3.8 и выше

```bash

pip install -r requirements.txt
```
или вручную:
```bash

pip install aiogram==3.28.2 python-dotenv
```
### 3. Получи токен для Telegram-бота

   1. Перейди к @BotFather в Telegram.
   2. Создай нового бота командой `/newbot`.
   3. Сохрани полученный токен.

### 4. Добавь токен в `.env`
Создай файл `.env` (в корне репозитория) и добавь строку:
`API_TOKEN=ваш_токен_бота`
### 5. Запусти бота:
```bash

python main.py
```
После первого запуска появится файл tasks_data.json, в котором хранятся задачи пользователей.

## 💡 Использование
### Пиши своему боту в Telegram команды:

- **/add** <текст> [дд.мм.гггг чч:мм] [low|normal|high]

    + Примеры:
/add сделать домашку 10.06.2026 21:00 high
/add купить молоко
- **/list** — показать все задачи

- **/done** <номер> — отметить задачу выполненной

    + /done 2

- **/edit** <номер> <новый текст> — изменить текст задачи

    + /edit 1 купить чёрный хлеб

- **/deadline** <номер> <дата/время> — изменить дедлайн

    + /deadline 2 11.06.2026 18:00

- **/priority** <номер> <low|normal|high> — изменить приоритет

    + /priority 2 low

- **/delete** <номер> — удалить задачу

    + /delete 1

- **/clear** — удалить все задачи

- **/search** <текст> — найти задачи по ключевому слову

    + /search хлеб

- **/help** — справка по командам

## 🔔 Напоминания и дедлайны
- Если у задачи есть дедлайн, бот автоматически напомнит о ней за 30 минут до срока.
- Напоминание придёт только один раз.
- Поддерживаемые форматы даты/времени:
    + дд.мм.гггг чч:мм (например, 10.06.2026 20:00)
    + дд.мм.гггг (если время не указано, по умолчанию 00:00)

## ⚙️ Файл данных
Все задачи хранятся в файле tasks_data.json в корне проекта.  
Файл создаётся автоматически и содержит задачи всех пользователей в виде словаря:

```json

{
  "123456789": [
    {
      "task": "Купить хлеб",
      "done": false,
      "priority": "normal",
      "deadline": "2026-06-10T20:00:00"
    }
  ]
}
```

## 🛠 Возможности доработки
- Инлайн-кнопки для управления задачами
- Поддержка английского языка
- Подключение к базе данных (SQLite/PostgreSQL)
- Поддержка групповых чатов
- Отправка напоминаний за разное время до дедлайна
- Экспорт/импорт задач
- ...и многое другое!

## 🤝 Вклад в проект
1. Форкни этот репозиторий
2. Сделай новую ветку: `git checkout -b feature/имя_фичи`
3. Внеси изменения
4. Сделай коммит: `git commit -am 'Добавил новую фичу'`
5. Отправь ветку: `git push origin feature/имя_фичи`
6. Оформи Pull Request

## 📂 Структура проекта
```
Telegram-TaskBot/  
│  
├── main.py           # основной код бота   
├── requirements.txt  # зависимости  
├── .gitignore        # игнорируемые файлы  
├── .env              # файл с токеном  
├── tasks_data.json   # файл с задачами (создается автоматически)  
├── README.md         # описание проекта   
```
